#include <iostream>
#include <vector>
#include <algorithm>
#include <cstdio>
using namespace std;

int main(int argc, char **args)
{
    vector<int> coll;

    for (int i =0 ; i< 200; ++i)\
        coll.push_back(i);

    sort(++coll.begin(), coll.end());

    int x = 1;
//    ++(x+1);

    int a, b;
    int tmp;
    void *tmp1;
//    while (tmp = scanf("%d %d", &a, &b))
//    {
//        cout << tmp << endl;
//    }
//    cout << tmp << endl;

//    while (cout << (tmp1 = cin >> a), tmp1);
//
    int n = 0;
    a = 0, b = 0;
//
//    switch(n)
//    {
//        case a+b:     ++n;
//        case 1:     ++n; cout << n << endl;
//        default:    ++n;    cout << n <<endl;
//    }
    w_char c;

    return 0;
}
